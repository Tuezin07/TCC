spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}
spring.profiles.group.prod=prod
spring.profiles.group.dev=dev

spring.datasource.url=${DATABASE_URL:jdbc:postgresql://dpg-d4dulhpr0fns73bbvrrg-a.oregon-postgres.render.com:5432/caixarapido_db}
spring.datasource.username=${DATABASE_USERNAME:caixarapido_db_user}
spring.datasource.password=${DATABASE_PASSWORD:Tdm1mkXLHeZnevpQIVGbLDH4MnZ1DXc7}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.datasource.hikari.maximum-pool-size=${DB_MAX_POOL:20}
spring.datasource.hikari.minimum-idle=${DB_MIN_IDLE:5}
spring.datasource.hikari.idle-timeout=${DB_IDLE_TIMEOUT:30000}
spring.datasource.hikari.connection-timeout=${DB_CONN_TIMEOUT:30000}
spring.datasource.hikari.max-lifetime=${DB_MAX_LIFETIME:1800000}
spring.datasource.hikari.data-source-properties.ssl=${DB_SSL:true}
spring.datasource.hikari.data-source-properties.sslmode=${DB_SSL_MODE:require}
spring.datasource.hikari.validation-timeout=5000
spring.datasource.hikari.leak-detection-threshold=2000

spring.jpa.hibernate.ddl-auto=${HIBERNATE_DDL:update}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=${SHOW_SQL:true}
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false
spring.jpa.properties.hibernate.enable_lazy_load_no_trans=true
spring.jpa.properties.hibernate.connection.provider_disables_autocommit=true

server.port=${PORT:8080}
server.servlet.context-path=/
server.connection-timeout=${SERVER_TIMEOUT:20000}
server.tomcat.max-threads=200
server.tomcat.min-spare-threads=20
server.tomcat.accept-count=100
server.compression.enabled=true
server.compression.mime-types=application/json,application/xml,text/html,text/xml,text/plain
server.compression.min-response-size=1024
server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=always

spring.jackson.time-zone=${APP_TIMEZONE:America/Sao_Paulo}
spring.jackson.locale=${APP_LOCALE:pt_BR}
spring.jpa.properties.hibernate.jdbc.time_zone=${DB_TIMEZONE:UTC}

logging.level.root=${LOG_LEVEL_ROOT:INFO}
logging.level.org.springframework.web=${LOG_LEVEL_SPRING_WEB:DEBUG}
logging.level.org.hibernate.SQL=${LOG_LEVEL_HIBERNATE_SQL:DEBUG}
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=${LOG_LEVEL_HIBERNATE_BINDER:TRACE}
logging.file.name=${LOG_FILE:app.log}
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %logger{36} - %msg%n
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} - %logger{36} - %msg%n

spring.webflux.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:*}
spring.webflux.cors.allowed-methods=${CORS_ALLOWED_METHODS:GET,POST,PUT,DELETE,OPTIONS}
spring.webflux.cors.allowed-headers=${CORS_ALLOWED_HEADERS:*}
spring.webflux.cors.allow-credentials=${CORS_ALLOW_CREDENTIALS:true}

mercadopago.access.token=${MERCADO_PAGO_ACCESS_TOKEN:dummy_token}

app.jwt.secret=${JWT_SECRET:umaChaveSecretaSupremaMuitoSegura123!}
app.jwt.expiration=${JWT_EXPIRATION:3600000}
app.jwt.refresh-expiration=${JWT_REFRESH_EXPIRATION:86400000}

spring.servlet.multipart.max-file-size=${MAX_FILE_SIZE:50MB}
spring.servlet.multipart.max-request-size=${MAX_REQUEST_SIZE:50MB}
spring.servlet.multipart.enabled=true

spring.cache.type=simple
spring.cache.cache-names=defaultCache,shortCache,longCache
spring.cache.simple.expire-after-write.defaultCache=600
spring.cache.simple.expire-after-write.shortCache=60
spring.cache.simple.expire-after-write.longCache=3600

spring.mail.host=${MAIL_HOST:smtp.mailtrap.io}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:username}
spring.mail.password=${MAIL_PASSWORD:password}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

server.ssl.enabled=${SSL_ENABLED:false}
server.ssl.key-store=${SSL_KEYSTORE:keystore.p12}
server.ssl.key-store-password=${SSL_KEYSTORE_PASSWORD:changeit}
server.ssl.key-store-type=${SSL_KEYSTORE_TYPE:PKCS12}
server.ssl.key-alias=${SSL_KEY_ALIAS:tomcat}
server.http2.enabled=true
server.tomcat.max-http-post-size=52428800
server.tomcat.accesslog.enabled=true
server.tomcat.accesslog.pattern=%h %l %u %t "%r" %s %b "%{Referer}i" "%{User-Agent}i"

management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always
management.endpoint.metrics.enabled=true
management.endpoint.prometheus.enabled=true
management.metrics.export.simple.enabled=true

app.feature.experimental=true
app.feature.debugMode=${FEATURE_DEBUG_MODE:false}

spring.main.allow-bean-definition-overriding=true
spring.main.lazy-initialization=true
spring.main.web-application-type=servlet
spring.main.banner-mode=off
