# Perfis
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# Database - Configuração simplificada para Render
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://dpg-d4dulhpr0fns73bbvrrg-a.oregon-postgres.render.com:5432/caixarapido_db}
spring.datasource.username=${DATABASE_USERNAME:caixarapido_db_user}
spring.datasource.password=${DATABASE_PASSWORD:Tdm1mkXLHeZnevpQIVGbLDH4MnZ1DXc7}
spring.datasource.driver-class-name=org.postgresql.Driver

# HikariCP - Configurações otimizadas para Render
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.max-lifetime=120000
spring.datasource.hikari.leak-detection-threshold=60000

# PostgreSQL SSL - Configuração correta
spring.datasource.hikari.data-source-properties.sslmode=require
spring.datasource.hikari.data-source-properties.ssl=true
spring.datasource.hikari.data-source-properties.sslfactory=org.postgresql.ssl.DefaultSSLFactory

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Desabilita configurações problemáticas
spring.jpa.properties.hibernate.connection.provider_disables_autocommit=false
spring.jpa.properties.hibernate.enable_lazy_load_no_trans=false

# Server
server.port=${PORT:8080}
server.servlet.context-path=/

# Logging - Aumente para DEBUG para troubleshooting
logging.level.com.caixarapido=DEBUG
logging.level.org.springframework.jdbc=DEBUG
logging.level.org.hibernate=INFO
logging.level.org.hibernate.SQL=DEBUG

# CORS
spring.web.cors.allowed-origins=*
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true

# Mercado Pago
mercadopago.access.token=${MERCADO_PAGO_ACCESS_TOKEN}

# Configurações simplificadas
spring.main.allow-bean-definition-overriding=true
spring.main.lazy-initialization=false